"""RecoveryAI — an autonomous, governed revenue-recovery agent.

The agent core (`recoveryai.core`) is a plain Python package with no web-framework
dependency. It can be embedded directly in a host application, or driven over HTTP
via the optional transport layer in `recoveryai.api`.
"""

__version__ = "2.0.0"
